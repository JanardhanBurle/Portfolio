import { BlurOnHoverDirective } from './blur-on-hover.directive';

describe('BlurOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
